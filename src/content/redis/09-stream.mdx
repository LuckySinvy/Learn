---
title: Stream：可靠消息队列
order: 9
description: XADD/XREAD/消费组/XACK/XPENDING/XAUTOCLAIM，与 List、PubSub 的对比
---

# Stream：可靠消息队列

List 做队列丢消息、不支持多消费组；Pub/Sub 不在线就收不到。Redis 5 引入的 Stream 补齐了这些短板：**消息持久保留、消费组分摊、ACK 确认、故障转移认领**——一个接近 Kafka 核心语义的日志结构。

## 1. 数据模型

Stream 是一个**只追加**的消息日志，每条消息有全局有序的 ID 和一组 field-value：

```text
stream: orders
+---------------------+----------------------------+
| ID                  | fields                     |
+---------------------+----------------------------+
| 1722310000000-0     | action=create sku=2001     |
| 1722310000000-1     | action=pay   order=8801    |   <- 同毫秒第二条, 序号+1
| 1722310005123-0     | action=ship  order=8801    |
+---------------------+----------------------------+
        ID 格式: 毫秒时间戳-序号, 严格递增
```

## 2. 生产与独立消费

### 2.1 XADD 写入

<Playground
  language="redis"
  title="XADD 生产与 XRANGE 回溯"
  id="redis-09-1"
  code={`# 星号表示让 Redis 自动生成 ID（毫秒时间戳-序号）
XADD orders * action create sku 2001
XADD orders * action pay order 8801
XLEN orders
# 控制长度：近似裁剪到 1 万条（波浪号表示允许略多，性能好）
XADD orders MAXLEN ~ 10000 * action ship order 8801
# 减号和加号是最小/最大 ID，读取全部消息
XRANGE orders - +`}
/>

### 2.2 XRANGE / XREAD 读取

`XRANGE key - +` 读取全部消息，`-`/`+` 分别代表最小与最大 ID，也可以传具体 ID 做范围查询。想要"只等新消息"则用阻塞读：

```bash
# 阻塞读取新消息: $ 表示"只要从现在开始的新消息"
127.0.0.1:6379> XREAD BLOCK 5000 STREAMS orders $
（挂起, 等到新消息或 5 秒超时）
```

XREAD 模式下每个消费者独立维护自己读到的位置，适合"每个消费者都要全量消息"的广播场景。

## 3. 消费组：分摊与确认

消费组让多个消费者**分摊**同一个 Stream 的消息（同组内一条消息只给一个消费者），并提供 ACK 机制。

### 3.1 创建组与消费

<Playground
  language="redis"
  title="消费组：XGROUP + XREADGROUP"
  id="redis-09-2"
  code={`# 这里手写 ID 便于后面 XACK 引用，生产中用星号自动生成
XADD orders 1-1 action create sku 2001
XADD orders 1-2 action pay order 8801
# 从头（0）开始消费；用美元符号则只消费此后的新消息
XGROUP CREATE orders g1 0
# 消费者 c1 领取 2 条新消息，大于号表示"从未投递给本组的消息"
XREADGROUP GROUP g1 c1 COUNT 2 STREAMS orders >
# 再读一次，已经没有新消息了
XREADGROUP GROUP g1 c1 COUNT 2 STREAMS orders >`}
/>

`>` 是特殊 ID，表示"给我从未投递给本组的新消息"。消息投递后进入该消费者的 **PEL（Pending Entries List，待确认列表）**，直到被 ACK。

### 3.2 确认与查看积压

<Playground
  language="redis"
  title="XACK 确认与 XPENDING 查积压"
  id="redis-09-3"
  code={`XADD orders 1-1 action create sku 2001
XADD orders 1-2 action pay order 8801
XGROUP CREATE orders g1 0
XREADGROUP GROUP g1 c1 COUNT 2 STREAMS orders >
# 处理完成，确认第一条
XACK orders g1 1-1
# 组内待确认概况：总数、最小/最大 ID、各消费者的挂起数
XPENDING orders g1
# 详细列表：每条的消费者、空闲毫秒数、投递次数
XPENDING orders g1 - + 10`}
/>

### 3.3 故障转移：XAUTOCLAIM

消费者 c1 崩溃后，它 PEL 里的消息不会自动重投。其他消费者用 `XAUTOCLAIM` 把"挂起超过一定时间"的消息认领过来：

<Playground
  language="redis"
  title="XAUTOCLAIM 认领僵尸消息"
  id="redis-09-4"
  code={`XADD orders 1-1 action pay order 8801
XGROUP CREATE orders g1 0
# c1 领走消息但不 ACK，模拟处理到一半崩溃
XREADGROUP GROUP g1 c1 COUNT 1 STREAMS orders >
XPENDING orders g1 - + 10
# c2 认领空闲超过 0ms 的消息（生产中填 60000 之类的阈值）
XAUTOCLAIM orders g1 c2 0 0
# 现在这条消息归 c2 了
XPENDING orders g1 - + 10`}
/>

消费者应定期跑 XAUTOCLAIM 作为"补偿任务"。投递次数（delivery count）随每次认领递增，超过阈值（如 5 次）仍失败的消息应转入死信队列（另一个 Stream）人工处理，避免毒消息无限循环。

### 3.4 完整消费循环（伪代码）

```python
while True:
    # 1. 优先处理新消息
    msgs = XREADGROUP GROUP g1 c1 COUNT 10 BLOCK 2000 STREAMS orders >
    for id, fields in msgs:
        try:
            process(fields)
            XACK orders g1 id
        except Exception:
            pass          # 不 ACK, 留在 PEL 等待重试/认领
    # 2. 周期性认领僵尸消息（也可由独立任务做）
    XAUTOCLAIM orders g1 c1 60000 0
```

## 4. 三种消息方案对比

| 能力 | List | Pub/Sub | Stream |
|---|---|---|---|
| 消息持久化 | 是（弹出即删） | 否，即发即失 | 是，可回溯 |
| 离线消息 | 保留 | 丢失 | 保留 |
| ACK 确认 | 无 | 无 | 有 |
| 消费组分摊 | 无 | 无 | 有 |
| 多组独立消费（广播） | 无 | 有 | 有（多个 group） |
| 阻塞消费 | BLPOP | SUBSCRIBE | XREAD BLOCK |
| 适用 | 简单任务队列 | 实时通知，可容忍丢失 | 可靠队列、事件流 |

选型直觉：**能容忍丢消息的实时广播用 Pub/Sub；一切"不能丢"的异步任务用 Stream**；List 只在极简单场景且不想引入消费组概念时使用。

## 5. 管理命令

```bash
127.0.0.1:6379> XINFO STREAM orders
 1) "length"
 2) (integer) 3
 3) "radix-tree-keys"
 4) (integer) 1
 ...
127.0.0.1:6379> XINFO GROUPS orders
1) 1) "name"
   2) "g1"
   3) "consumers"
   4) (integer) 2
   5) "pending"
   6) (integer) 1
   7) "last-delivered-id"
   8) "1722310000000-1"
127.0.0.1:6379> XDEL orders 1722310000000-0     # 删除指定消息
(integer) 1
127.0.0.1:6379> XTRIM orders MAXLEN ~ 1000      # 手动裁剪
(integer) 0
```

<Callout type="warn" title="Stream 不裁剪会吃光内存">
Stream 消息 ACK 之后也**不会被删除**（这是它能多组消费、可回溯的原因）。必须通过 XADD 的 MAXLEN/MINID 参数或定期 XTRIM 控制长度，否则一个高吞吐 Stream 会无限增长直至 OOM。带波浪号的 `MAXLEN ~ N` 是近似裁剪，按宏节点整块删除，性能远好于精确裁剪。
</Callout>

<Callout type="info" title="和 Kafka 的差距">
Stream 有消费组与 ACK，但没有分区（单 key 单线程处理，吞吐有单机上限）、没有多副本 ISR 语义（可靠性依赖 Redis 自身持久化与主从复制，主从切换仍可能丢尾部消息）。十万级 QPS 以下的业务队列足够；金融级或超大吞吐场景仍需 Kafka/Pulsar。
</Callout>

## 小结

- Stream 是只追加日志：XADD 生产，XRANGE 回溯，XREAD 独立消费
- 消费组三件套：XREADGROUP 领取（进入 PEL）、XACK 确认、XAUTOCLAIM 认领僵尸消息
- 投递次数超限的消息应转死信 Stream，防止毒消息循环
- 必须用 MAXLEN/XTRIM 控制长度；ACK 不等于删除
- 下一章回到 key 的生命周期：过期与内存淘汰 →

<Callout type="exercise" title="练习">
1. 创建一个 Stream 和消费组，用两个 redis-cli 窗口模拟两个消费者，验证消息被分摊而不是都收到。
2. 一个消费者领取消息后不 ACK，用 XPENDING 观察其挂起状态，再用 XAUTOCLAIM 从另一个消费者身份认领它。
3. 对比实验：Pub/Sub 的订阅者断开期间发布消息，重连后收不到；Stream 消费组的消费者离线期间的消息重连后仍能领取。

<Answer>
**参考答案：**

**1. 两个消费者分摊消息**

```bash
# ---- 准备：创建 Stream 和消费组 ----
127.0.0.1:6379> XADD orders '*' order_id 1001 amount 99
"1785000000000-0"
127.0.0.1:6379> XGROUP CREATE orders g1 0
OK
# 说明：0 表示从头开始消费；$ 表示只消费新消息
# 如果 Stream 还不存在，加 MKSTREAM 参数：XGROUP CREATE orders g1 0 MKSTREAM

# ---- 再生产 5 条消息 ----
127.0.0.1:6379> XADD orders '*' order_id 1002 amount 150
"1785000001000-0"
127.0.0.1:6379> XADD orders '*' order_id 1003 amount 200
"1785000002000-0"
127.0.0.1:6379> XADD orders '*' order_id 1004 amount 88
"1785000003000-0"
127.0.0.1:6379> XADD orders '*' order_id 1005 amount 320
"1785000004000-0"
127.0.0.1:6379> XLEN orders
(integer) 5

# ---- 窗口 A：消费者 c1 领取 2 条 ----
127.0.0.1:6379> XREADGROUP GROUP g1 c1 COUNT 2 STREAMS orders >
1) 1) "orders"
   2) 1) 1) "1785000000000-0"
         2) 1) "order_id"
            2) "1001"
            3) "amount"
            4) "99"
      2) 1) "1785000001000-0"
         2) 1) "order_id"
            2) "1002"

# ---- 窗口 B：消费者 c2 领取 2 条 ----
127.0.0.1:6379> XREADGROUP GROUP g1 c2 COUNT 2 STREAMS orders >
1) 1) "orders"
   2) 1) 1) "1785000002000-0"      # 拿到的是 1003、1004
         2) 1) "order_id"
            2) "1003"
      2) 1) "1785000003000-0"
         2) 1) "order_id"
            2) "1004"

# 关键：c2 拿到的是 c1 没拿过的消息，不是同一批 —— 这就是"分摊"
```

`>` 这个特殊 ID 是分摊的关键：

| ID 参数 | 含义 |
|---|---|
| `>` | 只取**从未被本组任何消费者领取过**的新消息 |
| `0` 或具体 ID | 取**本消费者自己**已领取但未 ACK 的历史消息（读 PEL） |

```bash
# ---- 验证：c1 用 0 只能看到自己的 PEL，看不到 c2 的 ----
127.0.0.1:6379> XREADGROUP GROUP g1 c1 COUNT 10 STREAMS orders 0
1) 1) "orders"
   2) 1) 1) "1785000000000-0"      # 只有 1001、1002，是 c1 自己领的
      ...

# ---- 查看组状态 ----
127.0.0.1:6379> XINFO GROUPS orders
1)  1) "name"
    2) "g1"
    3) "consumers"
    4) (integer) 2                  # 2 个消费者
    5) "pending"
    6) (integer) 4                  # 4 条已领取未 ACK
    7) "last-delivered-id"
    8) "1785000003000-0"

127.0.0.1:6379> XINFO CONSUMERS orders g1
1) 1) "name"
   2) "c1"
   3) "pending"
   4) (integer) 2
   ...
2) 1) "name"
   2) "c2"
   3) "pending"
   4) (integer) 2

# ---- ACK 后 PEL 减少 ----
127.0.0.1:6379> XACK orders g1 1785000000000-0 1785000001000-0
(integer) 2
```

**对比广播**：如果要让两个消费者都收到全部消息，就建**两个消费组**——组间广播、组内分摊。这是 Stream 比 List 强的核心能力。

```bash
127.0.0.1:6379> XGROUP CREATE orders g2 0
OK
127.0.0.1:6379> XREADGROUP GROUP g2 c1 COUNT 5 STREAMS orders >
# g2 的 c1 能拿到全部 5 条，因为 g2 的进度是独立的
```

**2. 不 ACK 的消息用 XPENDING 观察，再用 XAUTOCLAIM 认领**

```bash
# ---- c1 领取一条但故意不 ACK ----
127.0.0.1:6379> XADD orders '*' order_id 2001 amount 500
"1785000010000-0"
127.0.0.1:6379> XREADGROUP GROUP g1 c1 COUNT 1 STREAMS orders >
1) 1) "orders"
   2) 1) 1) "1785000010000-0"
         2) 1) "order_id"
            2) "2001"
# 这里模拟消费者崩溃：不执行 XACK

# ---- XPENDING 摘要模式：快速看整体挂起情况 ----
127.0.0.1:6379> XPENDING orders g1
1) (integer) 3                      # 总挂起数
2) "1785000002000-0"                # 最小挂起 ID
3) "1785000010000-0"                # 最大挂起 ID
4) 1) 1) "c1"
      2) "1"                        # c1 挂起 1 条
   2) 1) "c2"
      2) "2"                        # c2 挂起 2 条

# ---- XPENDING 明细模式：看每条消息的详情 ----
127.0.0.1:6379> XPENDING orders g1 - + 10
1) 1) "1785000002000-0"
   2) "c2"                          # 归属消费者
   3) (integer) 125300              # 空闲时间（毫秒），即多久没被处理
   4) (integer) 1                   # 投递次数
2) 1) "1785000010000-0"
   2) "c1"
   3) (integer) 45120
   4) (integer) 1

# 只看某个消费者的挂起消息
127.0.0.1:6379> XPENDING orders g1 - + 10 c1
1) 1) "1785000010000-0"
   2) "c1"
   3) (integer) 46200
   4) (integer) 1

# 只看空闲超过 30 秒的（IDLE 过滤，Redis 6.2+）
127.0.0.1:6379> XPENDING orders g1 IDLE 30000 - + 10

# ---- XAUTOCLAIM：以 c3 的身份认领 c1 的僵尸消息 ----
# 语法：XAUTOCLAIM key group 新消费者 最小空闲毫秒 起始ID [COUNT n]
127.0.0.1:6379> XAUTOCLAIM orders g1 c3 30000 0 COUNT 10
1) "0-0"                            # 下次扫描的起始游标，0-0 表示扫完了
2) 1) 1) "1785000002000-0"
      2) 1) "order_id"
         2) "1003"
   2) 1) "1785000010000-0"
      2) 1) "order_id"
         2) "2001"
3) (empty array)                    # 已被删除的消息 ID 列表

# ---- 验证归属已转移 ----
127.0.0.1:6379> XPENDING orders g1 - + 10
1) 1) "1785000002000-0"
   2) "c3"                          # 归属变成 c3
   3) (integer) 156                 # 空闲时间被重置
   4) (integer) 2                   # 投递次数 +1

# ---- c3 处理完后 ACK ----
127.0.0.1:6379> XACK orders g1 1785000002000-0 1785000010000-0
(integer) 2
```

**三个关键机制**：

- **PEL（Pending Entries List）**：每个消费组维护一份"已投递未确认"的清单。这是 Stream 实现"至少一次投递"的基础——只要没 ACK，消息就一直在 PEL 里，不会丢
- **`min-idle-time` 是安全阀**：只认领空闲超过阈值的消息。设成 30000 意味着"30 秒没动静才认为消费者挂了"，避免把正在被正常处理的消息抢走。这个值应该设为**正常处理耗时的几倍**
- **投递次数（delivery count）**：每次被认领都 +1。这是识别"毒消息"的依据

**毒消息处理**（生产必备）：

```bash
# XAUTOCLAIM 不返回投递次数，需要配合 XPENDING 明细判断
# 投递超过 3 次的消息转入死信 Stream，然后 ACK 掉原消息
127.0.0.1:6379> XADD orders:dlq '*' original_id "1785000010000-0" reason "retry_exceeded"
"1785000099000-0"
127.0.0.1:6379> XACK orders g1 1785000010000-0
(integer) 1
```

不做这一步的后果：一条永远处理失败的消息会被无限认领、无限重试，占满消费者的处理能力。

`XCLAIM` 是 `XAUTOCLAIM` 的手动版本，需要自己指定消息 ID；`XAUTOCLAIM`（Redis 6.2+）能自动扫描，是推荐做法。另外 `XCLAIM` 支持 `JUSTID` 选项，只转移归属不返回内容，适合只想重置计数的场景。

**3. Pub/Sub 与 Stream 的可靠性对比实验**

```bash
# ========== 实验一：Pub/Sub 会丢消息 ==========

# ---- 窗口 A：订阅 ----
127.0.0.1:6379> SUBSCRIBE news
Reading messages... (press Ctrl-C to quit)
1) "subscribe"
2) "news"
3) (integer) 1

# ---- 窗口 B：发布 ----
127.0.0.1:6379> PUBLISH news "msg-1"
(integer) 1                         # 返回 1 = 有 1 个订阅者收到

# ---- 窗口 A 立刻收到 ----
1) "message"
2) "news"
3) "msg-1"

# ---- 窗口 A 按 Ctrl-C 断开订阅 ----

# ---- 窗口 B 在 A 离线期间继续发布 ----
127.0.0.1:6379> PUBLISH news "msg-2"
(integer) 0                         # 返回 0 = 没有任何订阅者，消息直接丢弃
127.0.0.1:6379> PUBLISH news "msg-3"
(integer) 0

# ---- 窗口 A 重新订阅 ----
127.0.0.1:6379> SUBSCRIBE news
1) "subscribe"
2) "news"
3) (integer) 1
# 等待... 什么都收不到，msg-2 和 msg-3 永久丢失
```

`PUBLISH` 的返回值就是最好的证据：**它返回的是"收到消息的订阅者数量"，返回 0 意味着这条消息被直接丢弃了**。Pub/Sub 是纯粹的"广播即焚"，服务端不保存任何消息。

```bash
# ========== 实验二：Stream 消费组不丢消息 ==========

127.0.0.1:6379> XADD news:stream '*' content "msg-1"
"1785001000000-0"
127.0.0.1:6379> XGROUP CREATE news:stream g1 0
OK

# ---- 消费者 c1 上线，消费 msg-1 ----
127.0.0.1:6379> XREADGROUP GROUP g1 c1 COUNT 10 STREAMS news:stream >
1) 1) "news:stream"
   2) 1) 1) "1785001000000-0"
         2) 1) "content"
            2) "msg-1"
127.0.0.1:6379> XACK news:stream g1 1785001000000-0
(integer) 1

# ---- c1 "下线"（在 CLI 里就是什么都不做）----

# ---- 离线期间继续生产 ----
127.0.0.1:6379> XADD news:stream '*' content "msg-2"
"1785001001000-0"
127.0.0.1:6379> XADD news:stream '*' content "msg-3"
"1785001002000-0"

# ---- c1 "重连"后再次读取 ----
127.0.0.1:6379> XREADGROUP GROUP g1 c1 COUNT 10 STREAMS news:stream >
1) 1) "news:stream"
   2) 1) 1) "1785001001000-0"
         2) 1) "content"
            2) "msg-2"              # 离线期间的消息一条不少
      2) 1) "1785001002000-0"
         2) 1) "content"
            2) "msg-3"

# ---- 甚至可以回溯任意历史消息 ----
127.0.0.1:6379> XRANGE news:stream - +
1) 1) "1785001000000-0"
   2) 1) "content"
      2) "msg-1"                    # 已 ACK 的消息依然在 Stream 里
...
```

**为什么 Stream 能做到？** 因为消息**持久化存储在 Stream 这个数据结构里**，消费组只记录一个"已投递到哪"的游标（`last-delivered-id`）。消费者离线只是暂时不来取，游标停在原地，消息一直在那儿等着。

**核心对比表**：

| 维度 | Pub/Sub | Stream 消费组 |
|---|---|---|
| 消息存储 | 不存储，转发即焚 | 持久化在 Stream 中 |
| 离线消息 | **永久丢失** | 重连后可继续领取 |
| 消息确认 | 无 | XACK |
| 崩溃恢复 | 无 | PEL + XAUTOCLAIM |
| 历史回溯 | 不可能 | XRANGE 任意回溯 |
| 投递语义 | 至多一次 | 至少一次 |
| 多消费者 | 全部广播 | 组内分摊、组间广播 |
| 内存占用 | 极小 | 随消息累积增长，需 XTRIM |
| 适用场景 | 实时通知、配置推送、缓存失效广播 | 业务队列、任务分发、事件溯源 |

**选型结论**：**能丢的用 Pub/Sub，不能丢的用 Stream。** 判断标准很简单——如果这条消息丢了会造成业务损失（订单未处理、扣款未记账），就必须用 Stream。

**Stream 的两个必做运维项**：

```bash
# 1. 控制长度，否则内存无限增长（ACK 不等于删除！）
127.0.0.1:6379> XADD news:stream MAXLEN '~' 10000 '*' content "msg"
# ~ 表示近似裁剪，性能好得多，会保留略多于 10000 条

# 或者定期裁剪
127.0.0.1:6379> XTRIM news:stream MAXLEN '~' 10000
127.0.0.1:6379> XTRIM news:stream MINID '~' 1785000000000   # 按时间裁剪

# 2. 清理不再使用的消费者，避免 XINFO 里堆积僵尸条目
127.0.0.1:6379> XGROUP DELCONSUMER news:stream g1 c_old
(integer) 0     # 返回该消费者 PEL 中被删除的消息数，非 0 说明有消息丢失风险
```

最后提醒 `XTRIM` 的陷阱：**裁剪会直接删除消息，即使它还在某个消费组的 PEL 里**。如果消费严重滞后，激进的裁剪会导致消息永久丢失。所以 MAXLEN 的值要留足余量，并对消费延迟做监控。
</Answer>
</Callout>
